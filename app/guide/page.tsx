import type { Metadata } from "next";
import Link from "next/link";

export const metadata: Metadata = {
  title: "使い方",
  description:
    "Chord2MIDIの使い方ガイド。コード進行の作り方、オンコード（ベース音の変更）、拍数・BPMの設定、試聴、MIDIファイルの書き出し、テキスト入力での操作方法をまとめて解説します。chord progression MIDI generatorの使い方はこちら。",
};

export default function GuidePage() {
  return (
    <div className="c2m-page-wrap c2m-prose">
      <nav className="c2m-nav" style={{ justifyContent: "flex-start", marginBottom: 12 }}>
        <Link href="/">← ツールに戻る</Link>
      </nav>

      <h1>Chord2MIDIの使い方</h1>
      <p className="lead">
        コード進行を作って、試聴し、そのままMIDIファイルとしてダウンロードできる無料ツールです。
        ログイン不要、ブラウザだけで完結し、作ったコード進行はサーバーに送信されません。
      </p>

      <h2>1. コードを選ぶ</h2>
      <p>
        「コード名から選ぶ」タブでは、ルート音・コードタイプ・拍数をプルダウンから選ぶだけでコードを作れます。
      </p>
      <pre>C │ Major 7 │ 2拍 → 実際には Cmaj7 になります</pre>
      <p>
        音楽理論に詳しくない場合は「雰囲気から選ぶ」タブに切り替えると、「明るい」「切ない」「おしゃれ」といった
        言葉からコードを選ぶこともできます。
      </p>

      <h2>2. コードを追加する</h2>
      <p>
        「＋ コードを追加」を押すと新しいコードがリストの末尾に追加されます。各コードはいつでもルート音・
        コードタイプ・拍数を変更でき、上下ボタンで並び替え、ゴミ箱アイコンで削除できます。
      </p>

      <h2>3. オンコードを使う</h2>
      <p>
        各コードの「詳細設定」（歯車アイコン）を開くと、「ベース音」というプルダウンがあります。ここを
        ルート音以外に変更すると、コードの一番下に鳴る音だけを変えられます。
      </p>
      <pre>{`Cmaj7 を作成
　↓ 詳細設定 → ベース音 → E
Cmaj7/E になる`}</pre>
      <p>
        これは一般に「オンコード」と呼ばれるものです。ベース音を変えるだけで、同じコードの響きのまま
        低音だけを変化させることができます。
      </p>

      <h2>4. 拍数を設定する</h2>
      <p>各コードの長さは0.5拍単位で指定できます。</p>
      <pre>{`Cmaj7 2拍
Am7 2拍
Fmaj7 4拍`}</pre>

      <h2>5. BPMを設定する</h2>
      <p>
        画面上部のBPM欄でテンポを指定できます。ここで設定した値が、ブラウザでの試聴と書き出すMIDIファイルの
        両方に反映されます。
      </p>

      <h2>6. 試聴する</h2>
      <p>
        各コードの🔊アイコンでそのコードだけを試聴できます。「全体を試聴」ボタンを押すと、コード進行を
        設定したBPM・拍数の通りに最初から再生します。
      </p>

      <h2>7. MIDIを書き出す</h2>
      <p>
        「MIDIをダウンロード」を押すと、作成したコード進行が <code>chord-progression.mid</code> という
        ファイルとして保存されます。Ableton Live、Logic Pro、FL Studioなど、お使いのDAWに読み込んでご利用ください。
      </p>

      <h2>8. テキスト入力</h2>
      <p>
        「コード名で入力」タブでは、コード名と拍数をテキストで直接入力できます。1行につき1コードで、
        半角スペースで区切ります。
      </p>
      <pre>{`Cmaj7 2
Am7 2
Fmaj7 4
G7 2`}</pre>
      <p>オンコードも同じ形式で入力できます。</p>
      <pre>{`Cmaj7/E 2
G/B 2`}</pre>
      <p className="callout">
        かんたん入力とテキスト入力は同じデータを共有しているため、どちらで作っても、もう一方に切り替えれば
        同じコード進行がそのまま表示されます。
      </p>

      <Link href="/" className="c2m-back-link">← ツールでコード進行を作ってみる</Link>
    </div>
  );
}
